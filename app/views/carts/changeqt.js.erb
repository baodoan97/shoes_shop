if ("<%= @checkqt %>" == '1') {
    alert("only sell quantity <%= @resetvalue %>");
    $('input[name=item' + "<%= params[:item][:id] %>" + "<%= params[:item][:size] %>" + ']').val("<%= @resetvalue %>");
}
if ("<%= @checkqt %>" == '2') {
    alert("Sorry! Out of stock");
}else{
	  location.reload();
}