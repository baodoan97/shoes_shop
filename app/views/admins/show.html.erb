<%= render 'layouts/adminshome' %>

<div class="container" style="max-width: 40%;font-size:16px">
  <p id="notice"><%= notice %></p>
  <div class="row">
    <div class="" style="text-align:center;">
        <div class="panel panel-success" >
            <div class="panel-heading"><strong style="font-size:20px">Info Admins</strong></div>
            <div class="panel-body">
              <strong>ID:</strong>
              <%= @admins.id %><br>
              <strong>Email:</strong>
              <%= @admins.email %><br>
              <strong>Create at:</strong>
              <%= @admins.created_at %><br>
              <strong>Update at:</strong>
              <%= @admins.updated_at %><br>
            </div>
          </div>
    </div>
  </div>
  <%= link_to 'Edit', edit_admin_path(@admins) %> |
  <%= link_to 'Back', admins_path %>
</div>